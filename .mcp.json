{
  "mcpServers": {
    "passport": {
      "command": "bun",
      "args": [
        "run",
        "${CLAUDE_PLUGIN_ROOT}/src/mcp/server.ts",
        "--data",
        "${CLAUDE_PLUGIN_DATA}"
      ]
    }
  }
}
