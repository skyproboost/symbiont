/**
 * Загрузка ВСЕХ таблиц формулировок — одним импортом.
 *
 * Пара «русская формулировка → английская» регистрируется в момент загрузки
 * объявившего её модуля (см. i18n.ts): иначе процесс, который факты только
 * ЧИТАЕТ из журнала, о переводах не знал бы ничего. Механизм рабочий, но у него
 * есть тихий отказ: точка входа, забывшая импорт, не ломается — она просто
 * показывает русские строки в английском выводе. Замер поймал это разом в пяти
 * местах: MCP отдавал по-русски все 51 формулировку, стартовая сводка и JIT-срез
 * по 13, гейт-сообщения по 9. Снаружи всё выглядело переведённым.
 *
 * Поэтому список таблиц живёт здесь, а точки входа импортируют одну строку.
 * Отвергнуто: регистрировать из core/store.ts (её импортируют все, кто читает
 * факты, — соблазнительно) — тогда ядро потянуло бы за собой майнер и
 * верификаторы, то есть верхние слои, и порядок зависимостей перевернулся бы
 * ради экономии одной строки. Здесь инверсия видна и названа, а не спрятана.
 *
 * Модуль ничего не экспортирует: его смысл — побочный эффект загрузки.
 */
import '../miner/facts'
import '../miner/packs'
import '../layer1/facts1'
import '../verifiers/content'
